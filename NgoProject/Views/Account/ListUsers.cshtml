@model IEnumerable<AppModelUser>
@{
    ViewData["Title"] = "ListUsers";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h4>Users List</h4>

@*
@if (Model.Any())
{*@
@*    <a asp-action="Register" asp-controller="Account"
       class="btn btn-primary mb-3" style="width:auto">
        Add new user
    </a>*@
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                        <thead>
                            <tr>
                                <th>
                                       Name
                                </th>
                                <th>
                                        Phone Number
                                </th>
                                <th>
                                        Email
                                </th>
                            <th>
                                Edit
                            </th>
                            <th>
                               Delete
                            </th>
                            </tr>
                        </thead>
                        <tbody>
                        <tbody>
                                    @foreach (var user in Model)
                                    {
                                <tr>
                                    <td>
                                                @user.UserName
                                    </td>
                                    <td>
                                                @user.PhoneNumber
                                    </td>
                                    <td>
                                                @user.Email
                                    </td>

                                <td>
                                    <a asp-action="EditUser" asp-controller="Account" asp-route-id="@user.Id" class="btn btn-primary">Edit</a>

                                </td>
                                <td>
                                    <form method="post" asp-action="DeleteUser" asp-route-id="@user.Id">
                                        <button type="submit" class="btn btn-danger">Delete</button>
                                    </form>
                                </td>
@*                                <td>
                                    <form method="post" asp-action="Lock" asp-route-id="@user.Id">
                                        <button type="submit" class="btn btn-danger">Lock</button>
                                    </form>
                                </td>
                                <td>
                                    <form method="post" asp-action="UnLock" asp-route-id="@user.Id">
                                        <button type="submit" class="btn btn-danger">Unlock</button>
                                    </form>
                                </td>*@

                            </tr>
                                    }
                        </tbody>

                    </table>
                </div>
            </div>
        </div>
    </div>
@*else
{
    <div class="card">
        <div class="card-header">
            No users created yet
        </div>
        <div class="card-body">
            <h5 class="card-title">
                Use the button below to create a user
            </h5>
            <a class="btn btn-primary" style="width:auto"
               asp-controller="Account" asp-action="Register">
                Add new user
            </a>
        </div>
    </div>
}*@